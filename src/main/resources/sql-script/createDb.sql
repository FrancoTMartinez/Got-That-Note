CREATE TABLE Users (
    USER_ID varchar(11) NOT NULL AUTO_INCREMENT,
    EMAIL varchar(50) NOT NULL,
    USER_PASSWORD varchar(50) NOT NULL
);
CREATE TABLE Note(
	NOTE_ID varchar(11) NOT NULL,
	TITTLE varchar(11) NOT NULL,
	TEXT varchar(350),
);

